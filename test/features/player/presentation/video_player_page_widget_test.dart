import 'package:flutter_test/flutter_test.dart';

void main() {
testWidgets('VideoPlayerPage builds (skipped in CI due to media_kit DLLs)', (tester) async {
    // Skipped: media_kit requires native libmpv in CI environment.
  }, skip: true);
}